<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Playback capture requires RECORD_AUDIO, despite never opening the microphone. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <uses-feature android:name="android.software.leanback" android:required="true" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <application
        android:name=".AudioReactiveApplication"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="@string/app_name"
        android:icon="@drawable/app_icon"
        android:banner="@drawable/tv_banner"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="org.hyperion.audioreactive.action.TOGGLE" />
            </intent-filter>
            <intent-filter>
                <action android:name="org.hyperion.audioreactive.action.ON" />
            </intent-filter>
            <intent-filter>
                <action android:name="org.hyperion.audioreactive.action.OFF" />
            </intent-filter>
        </activity>
        <!-- Visible, separately bindable remote surfaces. They immediately forward a fixed action to MainActivity. -->
        <activity android:name=".RemoteOnActivity" android:exported="true" android:label="@string/remote_command_on" />
        <activity android:name=".RemoteOffActivity" android:exported="true" android:label="@string/remote_command_off" />
        <activity android:name=".RemoteToggleActivity" android:exported="true" android:label="@string/remote_command_toggle" />
        <!-- Internal-only capture toggle.  No third-party caller can repeatedly invoke consent/preflight. -->
        <activity android:name=".CaptureToggleActivity" android:exported="false" android:excludeFromRecents="true" android:noHistory="true" />

        <service android:name=".AudioReactiveService" android:exported="false" android:foregroundServiceType="mediaProjection" />
        <!-- App-launch control availability only: no boot receiver or sticky restart. -->
        <service android:name=".MqttControlService" android:exported="false" android:foregroundServiceType="connectedDevice" />

    </application>
</manifest>
